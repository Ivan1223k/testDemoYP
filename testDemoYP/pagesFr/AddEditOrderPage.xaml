<Page x:Class="testDemoYP.pagesFr.AddEditOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:testDemoYP.pagesFr"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Добавление/редактирование заказа"
      FontSize="14"
      Background="{StaticResource PrimaryBackground}">

    <Grid Margin="10" Background="{StaticResource PrimaryBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Дата заказа:" Margin="0,0,0,5"/>
        <DatePicker x:Name="OrderDatePicker" Grid.Row="1" Margin="0,0,0,10" Background="White"/>

        <TextBlock Grid.Row="2" Text="Дата доставки:" Margin="0,0,0,5"/>
        <DatePicker x:Name="DeliveryDatePicker" Grid.Row="3" Margin="0,0,0,10" Background="White"/>

        <TextBlock Grid.Row="4" Text="Статус:" Margin="0,0,0,5"/>
        <ComboBox x:Name="StatusComboBox" Grid.Row="5" DisplayMemberPath="StatusName" SelectedValuePath="ID_Status" Margin="0,0,0,10" Background="White"/>

        <TextBlock Grid.Row="6" Text="Адрес:" Margin="0,0,0,5"/>
        <ComboBox x:Name="AddressComboBox" Grid.Row="7" DisplayMemberPath="City" SelectedValuePath="ID" Margin="0,0,0,20" Background="White"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Click="SaveButton_Click" Margin="0,0,10,0" Width="80" Background="{StaticResource AccentBrush}"/>
            <Button Content="Отмена" Click="CancelButton_Click" Width="80" Background="{StaticResource SecondaryBackground}"/>
        </StackPanel>
    </Grid>
</Page>